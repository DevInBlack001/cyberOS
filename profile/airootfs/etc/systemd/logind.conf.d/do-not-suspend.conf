# Live ISO: never suspend/hibernate (RAM-backed root does not survive it, esp. in VMs).
# The installer removes this file and the masked sleep targets.
[Login]
HandleSuspendKey=ignore
HandleHibernateKey=ignore
HandleLidSwitch=ignore
HandleLidSwitchExternalPower=ignore
IdleAction=ignore
